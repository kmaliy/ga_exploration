# artifacts/

* `reports/` — committed evidence from Step 1 API exploration: the raw probe
  output, the generated profile report, and the exploration log.
* `samples/` — bulk pipeline output from `--dry-run` (JSONL). Gitignored:
  regenerate with `python data_pipeline.py run ... --dry-run`.
